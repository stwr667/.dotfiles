### Run the below to make sublime executable on the command line

```shell
ln -s "/Applications/Sublime Text.app/Contents/SharedSupport/bin/subl" ~/bin/sublime
```

This assumes `$HOME/bin` is in the PATH.
