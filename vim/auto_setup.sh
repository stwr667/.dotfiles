# NeoBundle
# if [ ! -d "$HOME/.vim/bundle/neobundle.vim" ]; then
#   curl https://raw.githubusercontent.com/Shougo/neobundle.vim/master/bin/install.sh | sh
# fi

# Makes this directory if it doesn't exist:
mkdir -p ~/vimBackupFiles
